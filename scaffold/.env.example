# Copy file này thành .env.local rồi điền giá trị thật
# Lấy trong Supabase Dashboard > Project Settings > API

NEXT_PUBLIC_SUPABASE_URL=https://yjctjvmmoyjhinqmrlye.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_9mWb7OjCRehXA20EcltweQ_dO4blYrA

# Dùng ở: (1) các script chạy tay trên máy (seed data, tạo ảnh...), và (2) route
# handler phía server app/api/create-child/route.ts (tạo tài khoản học sinh) — dùng
# để chạy "next dev" / "wrangler dev" trên máy. KHÔNG BAO GIỜ đưa vào code phía trình
# duyệt ('use client'). Khi đã deploy thật lên Cloudflare Worker, giá trị này CẦN được
# đặt lại bằng lệnh "wrangler secret put SUPABASE_SERVICE_ROLE_KEY" (xem
# HUONG-DAN-TRIEN-KHAI.md, Bước 11) — file .env.local KHÔNG tự động có mặt trên Worker
# đã deploy.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlqY3Rqdm1tb3lqaGlucW1ybHllIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4NjcxMzg1MywiZXhwIjoyMTAyMjg5ODUzfQ.U3IqXZJ9cCbgcdqefegXANWhoHi0ABpMkGriXlOkOmU
